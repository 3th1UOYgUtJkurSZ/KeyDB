# KeyDB active-replica 示例 —— 节点 B
# 关键顺序:active-replica 必须在 replicaof 之前
bind 0.0.0.0
port 6379
protected-mode no

# 开启 active-active(会自动隐含 replica-read-only no)
active-replica yes

# 指向对端节点 A(容器名 node-a,同一 docker 网络内可按名解析)
replicaof node-a 6379

# 演示用:关闭持久化以简化
save ""
appendonly no
logfile ""
